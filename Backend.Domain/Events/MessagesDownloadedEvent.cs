namespace Backend.Domain.Events;

public sealed record MessagesDownloadedEvent(int Count) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredAt { get; } = DateTime.UtcNow;
}
